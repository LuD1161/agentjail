# Plan 024 handoff

- Agent: /root/plan024_compose
- Work status: COMPLETE
- Acceptance verdict: PASS
- Starting HEAD: ce361898acc05c580c4186026b8819756ccbccfe
- Commit: this handoff is included in the task commit; SHA reported after commit
- Changed paths:
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/AgentjailApprovalApp.swift`
  - deleted `macos/AgentjailApproval/Sources/AgentjailApprovalApp/PlaceholderView.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/Composition/ApprovalAppComposition.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/Composition/ApprovalAppSceneViews.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/Settings/ApprovalLoginItemService.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/Settings/ApprovalSettingsView.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/Lifecycle/ApprovalApplicationDelegate.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/Lifecycle/ApprovalApplicationLifecycle.swift`
  - `macos/AgentjailApproval/Tests/AgentjailApprovalAppTests/Composition/ApprovalAppCompositionTests.swift`
  - `macos/AgentjailApproval/Tests/AgentjailApprovalAppTests/ScaffoldTests.swift`
  - `macos/AgentjailApproval/README.md`
  - `plans/macos-app/handoffs/024.md`
- Claim-time owned-path status: exact scoped `git diff` and `git status --short --untracked-files=all` were empty at `ce361898`; only the pre-existing inaccessible global-gitignore warning appeared. The orchestrator separately authorized the necessary one-line `ScaffoldTests.swift` replacement after its deleted-placeholder reference stopped the first harness compile.
- Implementation:
  - The application delegate builds and retains one production composition graph. It installs the strongly retained notification delegate and categories in `applicationWillFinishLaunching`, then starts the sole `ApprovalStore` poller in `applicationDidFinishLaunching`.
  - Authoritative notification snapshots enter one serial, coalescing synchronization loop. A later snapshot waits for the in-flight synchronization and is always processed last, so cancellation-insensitive notification-center work cannot restore stale scheduled requests.
  - The persistent menu-bar label owns public `OpenWindowAction` route bridges. Notification Review refreshes before it opens the singleton `approval-review` window; only that window receives and consumes the generation-stamped focus request. Review itself performs no mutation.
  - `MenuBarExtra` uses its public insertion binding. Removal stops the store and terminates the menu-only app without changing login registration or daemon state. A central one-shot termination guard makes repeated removal and Quit idempotent.
  - Settings keeps notification permission and `SMAppService.mainApp` registration as explicit user actions. It reports enabled, not-registered, approval-required, not-found, and unknown login status separately, and exposes the documented login-items settings route for approval-required status. No current documented public macOS per-app notification-settings deep link was established, so notification denial provides manual System Settings instructions; this remains a Plan 026 real-Mac check.
- Verification commands and exact results:
  - `rtk env GOTMPDIR=/private/tmp TMPDIR=/private/tmp go test ./internal/daemonapp -run 'TestResolvePeerCWD|TestDecideBoundCWD|TestGrant' -count=1` — PASS.
  - `rtk env TMPDIR=/private/tmp/agentjail-plan024-tmp GOCACHE=/private/tmp/agentjail-plan024-cache GOFLAGS=-mod=readonly go test ./internal/daemonapp ./internal/grantctl -run 'Test.*Review|Test.*Grant' -count=1` — PASS.
  - `rtk ./scripts/test-macos-approval.sh` — PASS on the final frozen-source run: macOS 13 production sources compiled/type-checked for arm64 and x86_64; 76 Core XCTest cases and 17 App XCTest cases passed, including 11 deterministic composition tests.
  - `rtk env TMPDIR=/private/tmp/agentjail-plan024-tmp GOCACHE=/private/tmp/agentjail-plan024-race-cache GOFLAGS=-mod=readonly go test ./internal/grantctl ./internal/daemonapp -race` — PASS. The standalone daemonapp race rerun also passed from cache.
  - Scoped forbidden-API/copy scans and `git diff --check` were run against Plan 024 production paths; no forbidden matches or whitespace errors were found.
- Acceptance criteria not exercised:
  - SwiftPM still cannot run in this nested environment because `xcrun` target-info cannot create its cache file. The accepted direct macOS harness compiled both target architectures and ran all discovered XCTest suites.
  - Real GUI interaction, physical notification delivery, login-item approval, and accessibility checks remain Plan 026 real-Mac gates.
- Risks / follow-ups: none known in the composition path.
- No remote action taken: yes
