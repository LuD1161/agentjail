# Plan 025 handoff

- Agent: /root/plan025_package
- Work status: COMPLETE
- Acceptance verdict: PASS
- Starting HEAD: f702630f4afb625042f05ddf52a14cf0ea91a936
- Commit: not committed; awaiting orchestrator review and release
- Changed paths:
  - `macos/AgentjailApproval/Resources/AgentjailApproval.entitlements`
  - `scripts/build-macos-approval-app.sh`
  - `scripts/package-macos-approval-dmg.sh`
  - `Makefile`
  - `macos/AgentjailApproval/README.md`
  - `test/macos-approval/packaging/verify.sh`
  - `plans/macos-app/handoffs/025.md`
- Claim-time owned-path status: exact scoped `git diff` produced no output;
  exact scoped `git status --short --untracked-files=all` produced no owned
  path output (only the pre-existing inaccessible global-gitignore warning and
  RTK `ok`).
- Tool versions:
  - macOS 26.2 (25C56)
  - Xcode 26.6 (17F113), selected explicitly through
    `/Applications/Xcode.app/Contents/Developer`
  - direct `swiftc --version` remained unavailable in this nested environment:
    `permissionDenied`; the explicit Xcode compiler successfully produced both
    release architectures
  - `codesign` and `hdiutil` system binaries were used by absolute path
- Implementation:
  - The approval-only build path canonicalizes and boundary-checks its artifact
    root before creating, replacing, or imaging anything. It rejects traversal
    and symlink escape paths, compiles deterministic Core/App source lists with
    the explicit Xcode `swiftc`, creates a static Core archive per architecture,
    produces arm64 and x86_64 macOS 13 thin executables, and combines them with
    `lipo`.
  - The direct compiler boundary pins the accepted `Package.swift` SHA-256 to
    `388d7e67eae25baa948ad517133c425e934be8c16ceb7f627ee5a793651af801`,
    retains semantic product/target checks, rejects dependencies/plugins/
    resources/unsafe flags, and rejects Core or task-path dylib linkage.
  - `AgentjailApproval.app` is ad-hoc signed with hardened runtime,
    `--timestamp=none`, and the exact empty entitlement plist. Verification
    requires `Signature=adhoc`, no `Timestamp=` record, the runtime flag, exact
    empty source and embedded dictionaries, strict per-executable and bundle
    signatures, plus supplemental deep verification.
  - The local DMG contains only the verified app and an `/Applications`
    symlink. It is checksumed, verified, attached read-only, inspected,
    reverified from its mount, and detached. It has no Developer ID,
    notarization, upload, or install behavior.
- Verification commands and exact results:
  - `rtk bash -n scripts/build-macos-approval-app.sh`,
    `rtk bash -n scripts/package-macos-approval-dmg.sh`, and
    `rtk bash -n test/macos-approval/packaging/verify.sh`: PASS.
  - `rtk shellcheck scripts/build-macos-approval-app.sh scripts/package-macos-approval-dmg.sh test/macos-approval/packaging/verify.sh`:
    PASS.
  - `rtk ./scripts/build-macos-approval-app.sh`: PASS. It built thin arm64 and
    x86_64 release executables with strict concurrency and warnings-as-errors,
    proved no Core/task-path dylib dependency, produced a universal app, and
    passed exact plist, entitlement, runtime, ad-hoc/no-timestamp, and strict
    signature checks.
  - `rtk ./test/macos-approval/packaging/verify.sh`: PASS. It rejects traversal
    and symlink roots, proves a copied manifest mutation changes the reviewed
    hash, runs two isolated builds, compares sorted `Contents` path/type
    manifests and normalized plists, verifies exact universal identity and
    signatures, and creates/verifies/mounts/detaches a local DMG with an exact
    `/Applications` link and mounted-app revalidation. Success cleanup removed
    its task roots.
  - `rtk ./scripts/package-macos-approval-dmg.sh`: PASS. Retained local
    artifacts: `build/macos-approval/AgentjailApproval.app` and
    `build/macos-approval/AgentjailApproval.dmg`; SHA-256:
    `e19621cc94dd245aca04373aca1cdbced45fdbcfc7f10e3043dcbab9faa47b78`.
    `hdiutil verify` passed; the read-only mounted copy reverified and `disk4`
    detached cleanly.
  - `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer ... xcodebuild -version`:
    PASS, reporting Xcode 26.6 (17F113).
  - `rtk /usr/sbin/spctl --assess --type execute --verbose=4 build/macos-approval/AgentjailApproval.app`:
    expected ad-hoc rejection (`rejected`); recorded as the local-only
    distribution limitation, not a success claim.
  - `rtk make -n macos-approval-app` and `rtk make -n macos-approval-dmg`:
    PASS; each resolves only its approval-specific script.
  - Final `rtk git diff --check` across all owned paths: PASS. Final scoped
    status contains only the expected Plan 025 paths; no path was staged.
  - Approval-script source scan for tunnel/notarization/Developer-ID/network
    publication patterns: PASS; no matches.
- Environment note: `lipo` and `otool` emitted non-fatal `xcrun_db-*`
  cache-file permission warnings in this nested environment. Their checks
  completed with status zero; the exact Xcode `swiftc` path compiled both
  architectures without relying on `swift --version`.
- Acceptance criteria not exercised: Developer ID signing, notarization,
  timestamp service access, Gatekeeper acceptance, installation, launch UX,
  and public distribution are deliberately deferred.
- Risks / follow-ups: none known in the local packaging path.
- No remote action taken: yes

## Follow-up: detach confirmation correction

- Work status: COMPLETE, pending orchestrator review.
- Scope: `scripts/package-macos-approval-dmg.sh`, deterministic tests under
  `test/macos-approval/packaging/`, `docs/GOTCHAS.md`, and this evidence-only
  handoff append.
- Security correction: exit-zero from `hdiutil detach` is no longer treated as
  sufficient. The script classifies the mount as active, absent, or
  indeterminate after every direct or cleanup-path detach; only an observed
  absent state clears `attached` and permits removal of the validated stage
  root. Active and indeterminate states preserve that root and return nonzero.
- Verification: `rtk bash -n scripts/package-macos-approval-dmg.sh`; `rtk bash
  -n test/macos-approval/packaging/detach_cleanup_test.sh`; and `rtk bash
  test/macos-approval/packaging/detach_cleanup_test.sh`: PASS. The deterministic
  fakes cover detach exit-zero while still mounted, mount-query failure,
  confirmed absence, TERM-triggered cleanup while still mounted, and attach
  nonzero with active or indeterminate media; unsafe cases prove the stage root
  is retained and receives no recursive-remove target, while success permits
  only the exact validated stage root as the recursive-remove target.
- At the deterministic correction gate, not exercised: real `hdiutil`, real
  `mount`, app build/launch, signing, network, remotes, and publication.
- No remote action taken: yes.

### Authorized local packaging recheck

- `rtk ./test/macos-approval/packaging/verify.sh`: PASS. The deterministic
  cleanup fakes passed first; two isolated universal builds then passed strict
  executable and bundle signature checks, exact empty-entitlement checks, and
  mounted-app revalidation. `hdiutil verify` reported the generated image
  valid, `hdiutil detach` reported `disk4` ejected, and the package script's
  post-detach tri-state check permitted only then its validated stage cleanup.
  The verifier reported `macos approval packaging: PASS` and its exact first
  task root no longer existed; a subsequent `mount` query had no matching path.
- The nested environment emitted known non-fatal `lipo`/`otool` xcrun-cache
  permission warnings, but every command returned success and all signature,
  image, mount, and cleanup assertions passed.
