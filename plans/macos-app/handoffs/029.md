# Plan 029 handoff

- Agent: plan017_protocol (Terra)
- Work status: COMPLETE
- Acceptance verdict: PASS
- Starting product baseline: `f3f10053422fd9839e13e311b1cd08906e7b39f3`
- Claim-time shared HEAD: `f10e5285` (orchestrator board/log changes only)
- Commit: this handoff is included in the task commit; SHA reported after commit
- Changed paths:
  - `internal/grantctl/registry.go`
  - `internal/grantctl/registry_test.go`
  - `internal/grantctl/review_test.go`
  - `internal/daemonapp/grantserver.go`
  - `internal/daemonapp/grantserver_test.go`
  - `docs/GOTCHAS.md`
  - `plans/macos-app/handoffs/029.md`
- Claim-time owned-path status: empty; Git emitted only the pre-existing warning
  that `/Users/aseemshrey/.config/git/ignore` was inaccessible.
- Authorized scope amendment: the orchestrator added
  `internal/grantctl/review_test.go` after claim. Its exact change passes the
  explicit `now` to `ListPending` and changes the one pre-reap assertion to
  require only the live grant. No v1 JSON field, fixture, projection rule, or
  unrelated review case changed.
- Implementation notes:
  - `Expires <= now` is centralized in one predicate. Claim, deny,
    coalescing, request-cap cleanup, and reaping share lock-held removal and
    index cleanup.
  - Claim checks expiry and transitions to claimed under one registry lock;
    expired claims return `ErrGrantExpired` with no snapshot or transaction
    closures.
  - List, review, find, deny, duplicate coalescing, and cap accounting all use
    explicit caller-provided time. Expired duplicates receive a fresh ID.
  - Daemon approval captures server time after authentication and makes the
    timed claim directly, removing the prior find/claim race while preserving
    durable-audit, binding, requested-audit, persistence, changed-audit, and
    commit ordering.
- Verification commands and exact results:
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go test ./internal/grantctl -run 'Test.*(Expir|Claim|RequestGrant|Review)' -count=1` — PASS (`ok`, 0.015s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go test ./internal/daemonapp -run 'Test.*(Grant|Expir)' -count=1` — PASS (`ok`, 0.029s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go test ./internal/grantctl ./internal/daemonapp -race -count=1` — PASS (`grantctl` 1.851s; `daemonapp` 9.612s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go vet ./internal/grantctl ./internal/daemonapp` — PASS (exit 0, no output).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly CGO_ENABLED=0 go build ./cmd/agentjail ./cmd/agentjail-daemon` — PASS (exit 0, no output).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go test ./internal/grantctl -run 'Test(ClaimGrant_ConcurrentBoundary|ClaimGrant_ExpiryBoundary|DenyGrant_ExpiryBoundary|RequestGrant_Expired)' -count=100` — PASS (`ok`, 0.129s).
  - Full non-race package runs passed: `internal/grantctl` (`ok`, 0.814s) and
    `internal/daemonapp` (`ok`, 5.889s).
  - `rtk git diff --check -- <owned product paths>` — PASS (exit 0, no output).
- Acceptance criteria not exercised: none.
- Risks / follow-ups: none known. A claim linearized before expiry may finish
  afterward; this is the accepted transaction rule in Plan 029.
- No remote action taken: yes
