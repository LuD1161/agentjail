# Plan 030 handoff

- Agent: plan017_protocol (Terra)
- Work status: COMPLETE
- Acceptance verdict: PASS
- Starting HEAD: `15af2111020c0f77b6097da514c95e4efccaa146`
- Commit: this handoff is included in the task commit; SHA reported after commit
- Changed paths:
  - `internal/grantctl/frame.go`
  - `internal/grantctl/frame_test.go`
  - `internal/grantctl/client.go`
  - `internal/grantctl/client_frame_test.go`
  - `internal/daemonapp/grantserver.go`
  - `internal/daemonapp/grantserver_frame_test.go`
  - `docs/GOTCHAS.md`
  - `plans/macos-app/handoffs/030.md`
- Claim-time owned-path status: empty; Git emitted only the pre-existing warning
  that `/Users/aseemshrey/.config/git/ignore` was inaccessible.
- Normative clarification after claim: orchestrator commit `894f6e89` permits
  bounded chunk read-ahead but forbids decoding/dispatching frame two and
  forbids a pre-auth per-byte read loop. The implementation uses fixed 4-KiB
  reads into exactly `MaxControlMsgBytes+1` storage, decodes only through the
  first LF, and discards bounded read-ahead.
- Authentication / dispatch-order audit:
  - `extractPeerUID` and `peerUIDAllowed` remain before request framing.
  - The old decoder expression alone became `ReadRequestFrame`; the existing
    `ctlauth.Valid` check remains immediately after it and before the timestamp,
    switch, registry, reload, or audit work.
  - The switch cases and their order are unchanged. Only `reply` moved from
    `json.Encoder` to `WriteResponseFrame`; the agent-facing socket is untouched.
- Implementation notes:
  - Typed request/response helpers distinguish missing delimiter, oversize,
    invalid UTF-8, malformed JSON, and trailing in-frame data while retaining
    additive unknown-field tolerance.
  - Complete frames include the LF in the 64-KiB bound. Writers marshal and
    size-check before a complete-write loop; an oversized response becomes one
    small parseable `ok=false` refusal before any original byte is written.
  - Client dial/reply deadlines and one-connection-per-operation behavior are
    unchanged. Both normal and slow round trips use the shared helpers.
- Verification commands and exact results:
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go test ./internal/grantctl -run 'Test.*Frame|TestRoundTrip' -count=1` — PASS (`ok`, 0.060s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go test ./internal/daemonapp -run 'Test.*Control.*Frame|Test.*Grant' -count=1` — PASS (`ok`, 0.029s).
  - The same focused commands with `-count=20` — PASS (`grantctl` 1.127s; `daemonapp` 0.193s).
  - Full non-race packages — PASS: `go test ./internal/grantctl -count=1` (`ok`, 0.875s) and `go test ./internal/daemonapp -count=1` (`ok`, 6.243s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go test ./internal/grantctl ./internal/daemonapp -race -count=1` — PASS (`grantctl` 1.905s; `daemonapp` 9.887s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly go vet ./internal/grantctl ./internal/daemonapp` — PASS (exit 0, no output).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-macos-go-tmp GOCACHE=/private/tmp/agentjail-macos-go-cache GOFLAGS=-mod=readonly CGO_ENABLED=0 go build ./cmd/agentjail ./cmd/agentjail-daemon` — PASS (exit 0, no output).
  - `rtk git diff --check -- <owned product paths>` — PASS (exit 0, no output).
- Acceptance criteria not exercised: none.
- Risks / follow-ups: none known. Bounded bytes read after the first delimiter
  are intentionally discarded and never decoded or dispatched, per the
  clarified Plan 030 contract.
- No remote action taken: yes
