# Plan 022 handoff

- Agent: `/root/plan017_protocol`
- Work status: COMPLETE
- Acceptance verdict: PASS
- Starting HEAD: `5bed3bc39c7c850a3786651b25570666abd0bd7c`
- Dependency integration HEAD: Plan 021 was accepted in `5c97c076594058d3e6c4427d50c79db130d8ae2b`; final verification ran with that product commit present.
- Commit: this handoff is included in the task commit; SHA reported after commit
- Changed paths:
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/UI/ApprovalMenuBarLabelView.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/UI/ApprovalPanelView.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/UI/ReviewCardView.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalCore/Presentation/ApprovalPresentation.swift`
  - `macos/AgentjailApproval/Tests/AgentjailApprovalCoreTests/Presentation/ApprovalPresentationTests.swift`
  - `plans/macos-app/handoffs/022.md`
- Claim-time owned-path status: `rtk git diff -- <owned paths>` and `rtk git status --short --untracked-files=all -- <owned paths>` were empty at claim HEAD. The six paths above are the only Plan 022 paths staged for this commit; unrelated user and Plan 023 work remains unstaged.
- Coordination note: the six already-verified owned paths were initially staged immediately after the orchestrator released its board/log commit, before this agent acquired the shared commit lock. No concurrent writer staged a path and the cached name/status remained exactly scoped. The agent then acquired the lock before the final handoff restage, cached-diff check, and signed commit.
- Presentation contract: `PanelPresentation` is a pure projection of the reviewed Plan 021 state. It preserves the daemon's canonical row order, distinguishes every ready/loading/stale/unauthorized/unsupported/error state without color-only meaning, bounds all untrusted reason and failure copy, and carries only typed display state into SwiftUI.
- Authority and actions: verified host/path context is shown exactly or downgraded to typed context-unavailable, Deny-only presentation; sanitized or partial authority is never actionable. Stale, expired, and in-flight rows disable both actions. Stable action failures remain row-attached and retryable when the authoritative row is otherwise actionable. View callbacks contain only `ReviewID`.
- Focus seam: an optional `ReviewFocusRequest` carries review ID plus monotonically distinct generation. The reusable panel focuses only an authoritative, unexpired row after projection. It reports bounded missing/expired terminal feedback and acknowledges each target or terminal route at most once per generation through the ID-plus-generation `onFocusConsumed` callback. A non-authoritative snapshot is deliberately not acknowledged, so the same route can become a target after refresh. No scene, notification, or app-composition mutation is present.
- Product copy: the approval title is exactly `Approve for future sessions`; the effect copy is exactly `Adds this host to the project policy for future sessions. The current session is unchanged.` No current-session approval is implied.
- Verification commands and exact results:
  - `rtk ./scripts/test-macos-approval.sh`: PASS in the one orchestrator-authorized full direct harness run. Production modules compiled/type-checked for macOS 13 on arm64 and x86_64. Core XCTest ran 76 tests with 0 failures, including 15 `ApprovalPresentationTests`; app XCTest ran 6 tests with 0 failures. The harness removed `/private/tmp/agentjail-macos-approval-tests-032.DmXBMTf1` on exit.
  - `rtk rg -n 'Allow once|Allow now|temporary|current session' macos/AgentjailApproval/Sources/AgentjailApprovalApp/UI macos/AgentjailApproval/Sources/AgentjailApprovalCore/Presentation/ApprovalPresentation.swift`: PASS. The only matches are the required truthful effect copy and its matching accessibility hint; no misleading approval copy appears.
  - Exact-copy scan for `Approve for future sessions` and `Adds this host to the project policy for future sessions. The current session is unchanged.`: PASS in the presentation contract and regression tests.
  - Boundary scan for `openWindow|OpenWindowAction|MenuBarExtra|NSApplication|UNUserNotificationCenter|SMAppService|UserNotifications|AppKit|os_log|Logger\(|slog|print\(` across owned production paths: PASS; no matches.
  - Sensitive-data scan found only bounded static diagnostics referring generically to the local control token; there are no token values, request/response bodies, environment values, logs, audit events, or payload rendering.
  - Presentation regressions cover the full state/action matrix, exact approval copy, verified and downgraded authority, inclusive expiry, stale/in-flight/failure behavior, bounded messages, canonical equal-timestamp order, truncation metadata, exact menu-label counts, generation-stamped focus transitions, and the focus-consumption policy.
- Acceptance criteria not exercised automatically: Plan 026 retains real VoiceOver, Voice Control, Switch Control, full-keyboard-access, Accessibility Inspector, increased-contrast, reduced-motion, and long-Unicode layout checks. Plan 024/026 must also exercise the same panel through the real supplemental notification window route. SwiftPM remains unavailable in this nested environment because the Xcode target-info path returns `permissionDenied`; the accepted direct `swiftc`/`xctest` harness compiled both target architectures and ran the complete discovered suites instead.
- Risks / follow-ups: no known implementation blocker. Window ownership, route clearing, app lifecycle, and notification activation intentionally remain outside this presentation-only commit. The consumer must provide monotonically distinct focus generations and clear a route only after `onFocusConsumed` confirms terminal or target handling.
- No remote action taken: yes
