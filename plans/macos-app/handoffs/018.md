# Plan 018 handoff

- Agent: plan017_protocol
- Work status: COMPLETE
- Acceptance verdict: PASS
- Starting HEAD: `afec1180dfa9cfd9e17ee7926b847e93e67c528f`
- Commit: this handoff is included in the task commit; SHA reported after commit
- Changed paths:
  - `internal/daemonapp/grantserver.go`
  - `internal/daemonapp/grantserver_review.go`
  - `internal/daemonapp/grantserver_review_test.go`
  - `plans/macos-app/handoffs/018.md`
- Claim and collision check:
  - The orchestrator acknowledged the exact four-path ownership claim at
    starting HEAD `afec1180`.
  - Claim-time scoped status, worktree diff, and index diff were empty; Git
    emitted only the pre-existing inaccessible global-gitignore warning.
  - Shared HEAD later advanced independently to `ab3dc633`; the committed diff
    from the starting HEAD across all claimed paths remained empty. Unrelated
    user/agent dirty paths were preserved and excluded from staging.
- Implementation:
  - Added the daemon-consumer `reviewSnapshotProjector` seam, defaulted to the
    existing typed `grantctl.Registry`; projection, sorting, expiry, binding,
    caps, truncation, and v1 domain semantics remain owned by `grantctl`.
  - Added one `ReqReviewSnapshot` case in `handleCtlConn` after the unchanged
    peer-UID check, strict bounded frame read, and control-token gate. The
    already captured server `now` is passed once to the projector and its typed
    `ReviewSnapshotV1` is returned without reconstruction.
  - Missing version zero and unsupported future versions return distinct,
    bounded, non-sensitive refusals without invoking the projector.
  - The read-only endpoint emits no audit event and adds no log statement. No
    hook/evaluation path, frame helper/client, registry behavior, Swift source,
    persistence, or approval/deny semantics changed.
- Acceptance coverage:
  - Exact bound and unbound projections use the daemon-supplied timestamp;
    verified `BoundCWD` is authoritative and a Darwin-shaped unbound request is
    deny-only with no self-reported CWD/session leakage.
  - Missing/invalid tokens and missing/future versions are rejected before a
    projector spy can run; an invalid typed version, malformed JSON, trailing
    value, and oversized frame also never dispatch.
  - The canonical v1 golden response fixture is emitted byte-for-byte through a
    real temporary Unix socket. Unknown additive request fields are tolerated,
    while two frames on one connection project only the first.
  - Empty queue is a valid typed snapshot and remains distinct from an
    unavailable daemon. Five pending reviews deterministically cap to three
    newest items with `total_pending=5` and `truncated=true`.
  - Real-socket concurrent snapshot calls race safely with one approval and one
    denial. A focused compatibility test confirms grant list, approve, deny,
    and daemon reload still work.
- Verification commands and exact results:
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-plan018-tmp GOCACHE=/private/tmp/agentjail-plan018-cache GOFLAGS=-mod=readonly go test ./internal/daemonapp ./internal/grantctl -run 'Test.*Review|Test.*Grant' -count=1` — PASS (`daemonapp` 0.035s, `grantctl` 0.012s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-plan018-tmp GOCACHE=/private/tmp/agentjail-plan018-cache GOFLAGS=-mod=readonly go test ./internal/daemonapp -run '^TestReviewSnapshot' -count=20` — PASS (`daemonapp` 0.219s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-plan018-tmp GOCACHE=/private/tmp/agentjail-plan018-race-cache GOFLAGS=-mod=readonly go test ./internal/daemonapp ./internal/grantctl -race` — PASS (`daemonapp` 17.835s, `grantctl` 1.909s).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-plan018-tmp GOCACHE=/private/tmp/agentjail-plan018-vet2-cache GOFLAGS=-mod=readonly go vet ./internal/daemonapp ./internal/grantctl` — PASS (exit 0, no output).
  - `rtk proxy env TMPDIR=/private/tmp/agentjail-plan018-tmp GOCACHE=/private/tmp/agentjail-plan018-build2-cache GOFLAGS=-mod=readonly CGO_ENABLED=0 go build ./cmd/agentjail ./cmd/agentjail-daemon` — PASS (exit 0, no output).
  - `rtk rg -n 'slog|\\.Emit\\(|audit\\.|CtlToken|Reason|Host|ProjectPath' internal/daemonapp/grantserver_review.go` — PASS (expected no matches; exit 1).
  - `rtk git diff afec1180..HEAD -- <claimed paths>` — PASS (no committed overlap).
- Risks / follow-ups: none known. Future review protocol versions should add an
  explicit typed handler branch rather than treating zero or unknown versions
  as v1.
- No remote action taken: yes
