# Plan 019 handoff

- Agent: plan019_prep
- Work status: PARTIAL
- Acceptance verdict: PARTIAL
- Starting HEAD: e1220db91d13e43482d2529b42e5d419050f6b18
- Commit: this handoff is included in the task commit; SHA reported after commit
- Changed paths:
  - `macos/AgentjailApproval/Package.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalCore/BuildMarker.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/AgentjailApprovalApp.swift`
  - `macos/AgentjailApproval/Sources/AgentjailApprovalApp/PlaceholderView.swift`
  - `macos/AgentjailApproval/Tests/AgentjailApprovalCoreTests/BuildMarkerTests.swift`
  - `macos/AgentjailApproval/Tests/AgentjailApprovalAppTests/ScaffoldTests.swift`
  - `macos/AgentjailApproval/Resources/Info.plist`
  - `macos/AgentjailApproval/README.md`
  - `plans/macos-app/handoffs/019.md`
- Claim-time owned-path status: no diff, staged, modified, or untracked output
  for the owned paths.
- Verification commands and exact results:
  - `rtk env TMPDIR=/tmp CLANG_MODULE_CACHE_PATH=/private/tmp/agentjail-approval-clang-module-cache SWIFT_MODULE_CACHE_PATH=/private/tmp/agentjail-approval-swift-module-cache swift package --disable-sandbox --package-path macos/AgentjailApproval --scratch-path /private/tmp/agentjail-approval-swiftpm describe`: passed. It reports macOS 13.0, products `AgentjailApprovalCore` and `AgentjailApproval`, and the two production plus two test targets. The nested sandbox still printed the non-fatal `xcrun_db-*` cache warning.
  - `rtk env TMPDIR=/tmp CLANG_MODULE_CACHE_PATH=macos/AgentjailApproval/.build/clang-module-cache SWIFT_MODULE_CACHE_PATH=macos/AgentjailApproval/.build/swift-module-cache swift test --disable-sandbox --package-path macos/AgentjailApproval --scratch-path macos/AgentjailApproval/.build`: blocked before compilation. The nested sandbox denied writing SwiftPM's `AgentjailApprovalApp.build/output-file-map.json`.
  - The equivalent `swift build --product AgentjailApproval` command with the same local cache and scratch settings stopped at the same nested-sandbox `output-file-map.json` denial.
  - `rtk env TMPDIR=/tmp swiftc -parse` on the core and app source files: passed.
  - `rtk plutil -lint macos/AgentjailApproval/Resources/Info.plist`: `OK`; `PlistBuddy` returned every required bundle value.
  - Forbidden-import scan returned no `NetworkExtension`, `UserNotifications`, `ServiceManagement`, `socket(`, or `Process(` matches in sources or tests.
  - The manifest contains no `.package(url:)` entry and no package-level dependency list. Its three `dependencies:` occurrences are the required local target dependencies; the plan's broad dependency grep also matches those local declarations.
  - `rtk git diff --check`: passed.
- Acceptance criteria not exercised: SwiftPM test and product build need rerun in a normal unsandboxed terminal. The execution environment permits the shell to create a scratch path but denies Swift compiler subprocess writes there, including with SwiftPM's `--disable-sandbox` option.
- Risks / follow-ups: Plan 025 assembles and signs the `.app`; no bundle
  assembly is part of this SwiftPM scaffold.
- No remote action taken: yes
