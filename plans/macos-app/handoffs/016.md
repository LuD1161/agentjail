# Plan 016 handoff

- Agent: plan016_prep
- Work status: PARTIAL
- Acceptance verdict: PARTIAL
- Starting HEAD: e1220db91d13e43482d2529b42e5d419050f6b18
- Commit: this handoff is included in the task commit; SHA reported after commit
- Changed paths:
  - `internal/daemonapp/peerpid_darwin.go`
  - `internal/daemonapp/peerpid_darwin_test.go`
  - `internal/daemonapp/grantserver.go`
  - `internal/daemonapp/grantserver_test.go`
  - `docs/GOTCHAS.md`
  - `plans/macos-app/handoffs/016.md`
- Claim-time owned-path status: no diff, staged, modified, or untracked output
  for any owned path.
- ABI sources: verified 2026-08-15 against Apple XNU
  [`proc_info_private.h`](https://raw.githubusercontent.com/apple-oss-distributions/xnu/main/bsd/sys/proc_info_private.h)
  (`PROC_INFO_CALL_PIDINFO = 0x2`),
  [`proc_info.h`](https://raw.githubusercontent.com/apple-oss-distributions/xnu/main/bsd/sys/proc_info.h)
  (vnode-path layout and flavor 9), and
  [`syscalls.master`](https://raw.githubusercontent.com/apple-oss-distributions/xnu/main/bsd/kern/syscalls.master)
  (`proc_info` syscall 336). The installed macOS 26.2 SDK independently
  confirmed the public layout and selector values available there.
- Verification commands and exact results:
  - `rtk env GOTMPDIR=/private/tmp TMPDIR=/private/tmp go test ./internal/daemonapp -run 'TestResolvePeerCWD|TestDecideBoundCWD|TestGrant' -count=1`: pass.
  - `rtk env GOTMPDIR=/private/tmp TMPDIR=/private/tmp go test ./internal/daemonapp -race -run 'TestResolvePeerCWD|TestDecideBoundCWD|TestGrant' -count=1`: pass.
  - `rtk env GOTMPDIR=/private/tmp TMPDIR=/private/tmp go vet ./internal/daemonapp`: pass.
  - `rtk env GOTMPDIR=/private/tmp CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go test -c -o /private/tmp/agentjail-daemonapp-arm64.test ./internal/daemonapp`: pass.
  - `rtk env GOTMPDIR=/private/tmp CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go test -c -o /private/tmp/agentjail-daemonapp-amd64.test ./internal/daemonapp`: pass.
  - `rtk env GOTMPDIR=/private/tmp CGO_ENABLED=0 go build ./cmd/agentjail ./cmd/agentjail-daemon`: pass.
  - Focused verbose Darwin run passed self CWD, child CWD with spaces/non-ASCII,
    invalid/exited PID, decoder validation, and the real permission boundary:
    PID 1 returned `operation not permitted`.
  - `rtk git diff --check`: pass.
- Acceptance criteria not exercised:
  - Full `go test ./internal/daemonapp -count=1` and full `go test ./internal/daemonapp -race` fail only in pre-existing, unowned
    `TestDaemon_SIGHUP_MCPDecisionChanges` and
    `TestDaemon_SIGHUP_FailureKeepsOldPolicy`. Their spawned daemon inherits
    the host home and cannot create a log directory because
    `~/.agentjail` is a file; the daemon socket therefore never appears.
    The focused Plan 016 tests and race subset pass. A separate scoped
    test-hygiene fix is required before full-package acceptance can be green.
- Risks / follow-ups: none in the owned resolver/binding path; retain the
  layout assertions and live child test when Darwin SDK/XNU changes.
- No remote action taken: yes
