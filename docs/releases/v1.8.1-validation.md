# v1.8.1 validation

Date: 2026-09-07 (America/Los_Angeles).

## Scope

Prepared from the published v1.8.0 commit, 04807744. This patch contains CLI
help and explicit Codex-launch compatibility fixes. It does not include the
unrelated local MCP-policy, capture, grant, or desktop changes.

## Completed locally

- `go build ./...` and `go vet ./...`.
- `go test -count=1 ./...`, including launch argument and help regressions.
- `make smoke`: hook fixtures 9 passed; shield fixtures 9 passed, with the
  existing optional proxy-not-running fixture skipped. Hook p95 was 10 ms.
- `make opa-test`: 502 policy tests passed.
- `make ui licenses-check adr-check`: build and attribution/ADR checks passed.
- Installed Codex 0.153.4: the explicit shielded bypass launch reached the native
  policy approval dialog; cancellation prevented the requested execution.

An initial Go-suite run hit the existing host-proxy cancellation test's PID-file
write race. Twenty targeted reruns and the subsequent uncached full-suite run
passed. This patch does not change that test or host-proxy behavior.

## Clean-VM gate exception

`make e2e-release` was attempted twice. Resource admission refused before
starting a VM: available host memory was 4.5 and 4.3 GiB, below the required
6 GiB (4 GiB guest plus 2 GiB host reserve).

The maintainer explicitly authorized committing and releasing this patch on
the local validation instead of repeating the VM gate. No resource limits were
lowered. This is a release-specific waiver, not a passing clean-VM result and
not a change to the normal release gate.
